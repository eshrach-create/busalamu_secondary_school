name: busalamu_secondary_school
description: Busalamu Secondary School Attendance
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.32.0
  cloud_firestore: ^4.17.0
  mobile_scanner: ^5.2.3
  file_picker: ^8.1.6
  excel: ^4.0.8
  printing: ^5.12.0
  url_launcher: ^6.2.5
  qr_flutter: ^4.1.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true